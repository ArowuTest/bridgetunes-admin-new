{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    // catch-all to index.html for an SPA
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
