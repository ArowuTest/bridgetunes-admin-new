// ─── src/App.js ─────────────────────────────────────────────────────────────

/**
 * Application entry point.
 * We now import from './App' (which will resolve to App.tsx)
 * rather than re-export the raw .tsx file.
 */

export { default } from './App';
